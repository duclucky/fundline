---
name: v98store-api
description: Deprecated compatibility pointer for Fundline's former workflow provider guidance. Use only when old project notes explicitly mention v98store, then follow the CheapKeyAI integration skill.
---

# Deprecated provider guidance

Fundline no longer uses v98store for workflow model calls. Read `.agents/skills/cheapkeyai-api/SKILL.md` and use the shared CheapKeyAI client, model registry, environment variables, and tests.

Do not restore old provider keys, endpoints, model modules, or runtime fallbacks.
